import XCTest
@testable import SSSwiftUISpinnerButton

final class SSSwiftUISpinnerButtonTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SSSwiftUISpinnerButton().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
